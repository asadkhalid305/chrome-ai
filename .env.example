# Reveal the Chrome-specific intro and per-API availability details.
# false (default): everything that names Chrome stays hidden, so a live
#   audience isn't told upfront that these lessons use Chrome built-in AI APIs.
# true: the intro and API details are always shown.
VITE_REVEAL_CHROME=false
